{
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && babel src --out-dir lib  --copy-files",
    "prepublish": "yarn run build"
  },
  "dependencies": {
    "stampy": "^0.12.0",
    "goose-css": "^0.1.0"
  }
}
